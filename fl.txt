AXIOM 
   Branch(length,10,width,5);
CONSTANTS
   RotX(theta),RotZ(theta),StartBrack(),EndBrack();
PRODUCTIONS
   Branch(length,width)(length>width & width>0.1):=Branch(length,length,width,width)Branch(length,length~6.28,width,width/1.1)Apex(length,length/3,width,width/1.5);
   Apex(length,width)(length==length):=StartBrack()RotZ(theta,3.14/2)Branch(length,length,width,width)EndBrack()
                                       StartBrack()RotZ(theta,-3.14/2)Branch(length,length,width,width)EndBrack()
                                       StartBrack()RotX(theta,3.14/2)Branch(length,length,width,width)EndBrack()
                                       StartBrack()RotX(theta,-3.14/2)Branch(length,length,width,width)EndBrack();
END

GRAPHICS
   RotZ(theta):=GLSRotateZ(theta);
   RotX(theta):=GLSRotateX(theta);
   Branch(length,width):=GLSWidth(width)GLSMove(length);
      
END
