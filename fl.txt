AXIOM 
   Branch(length,1,width,0.5)Apex(length,0.5,width,0.5);
CONSTANTS
   RotX(theta),RotZ(theta),StartBrack(),EndBrack(),Branch(length,width);
PRODUCTIONS
   Apex(length,width)(length==length):=
                                       StartBrack()RotZ(theta,3.14/2)Branch(length,length,width,width)Apex(length,length/3,width,width/1.5)EndBrack()
                                       StartBrack()RotZ(theta,-3.14/2)Branch(length,length,width,width)Apex(length,length/3,width/width/1.5)EndBrack()
                                       StartBrack()RotX(theta,3.14/2)Branch(length,length,width,width)Apex(length,length/3,width/width/1.5)EndBrack()
                                       StartBrack()RotX(theta,-3.14/2)Branch(length,length,width,width)Apex(length,length/3,width/width/1.5)EndBrack()
                                       Branch(length,length,width,width)Apex(length,length/2,width,width/1.3);
END

GRAPHICS
   RotZ(theta):=GLSRotateZ(theta);
   RotX(theta):=GLSRotateX(theta);
   Branch(length,width):=GLSWidth(width)GLSMove(length);
      
END
